# 性能热点诊断 · 2026-09-14

主要瓶颈在 CPU 侧围棋特征及输入投影的重复计算。搜索中，完整重建累加器占约 74%；
训练中，CPU 打包占约 53%。初次诊断只添加计时工具；下方历史诊断与行号对应优化前代码。最新优化结果如下。


## 训练打包预取实验 · v31

以独立随机初始化的隐藏宽度 128 模型，16 次模拟自博弈 64 局生成 9784 条新 MC 样本。没有转换旧 TD 数据，也不写入正式模型或回放路径。
实现有界打包预取，0 为串行，1/2/4 为打包线程数，保序训练；每个线程队列容量 1。
没有实现特征缓存，没有修改学习率、batch 或搜索预算。默认选择 4，可用 `GO9_TRAIN_PACK_WORKERS=0` 恢复串行。

三次独立进程中位数，未开启 profile：

| 场景 | 串行 | 4 线程预取 | 变化 |
| --- | ---: | ---: | ---: |
| 独立训练：抽取 2048 条、40 epoch、batch=256 | 31081 样本/s | 33967 样本/s | +9.3% |
| 128 个固定局面搜索线程：81920 条、1 epoch、batch=256 | 3070 样本/s | 4862 样本/s | +58.4% |
| 上述单 epoch 压力测试的搜索吞吐 | 725838 访问/s | 728804 访问/s | +0.4% |

单 epoch 数据由同一 MC 数据集循环取 81920 条，内存展开在计时前完成。训练预热后计时，使用同样的模型、样本顺序和优化器步数。
压力测试包括启动及等待搜索线程结束的时间，搜索使用固定模型与五个固定局面、每次 400 模拟。
其负载与 128 actors 完整自博弈不同，尤其不包括真实采样增强、局面分布变化和版本发布，不能直接换算用户日志吞吐。
压力场景串行三次为 2989/3070/3280 样本/s，4 线程为 4862/5181/4472；最终打印 loss 均约 3.5993。

先期的 2048 条 × 40 epoch 压力试验中，串行/2线程/4线程中位数为 3618/3080/2831 样本/s，预取反而退步。
该设置每个 epoch 重建打包线程，与当前 batch_epochs=1 的生产配置不同；这提示收益依赖批次数量和线程竞争，不能只凭独立训练结果启用。
独立训练 1/2 线程的中位数分别为 29124/31483 样本/s。

验证：打包内容逐字段一致，含末尾不满 batch；CPU 训练参数对照、提前停止与异常退出通过。
GPU 参数对照在整组并发测试中曾超出 1e-5 容差，单独重跑通过；最终采用确定性的 CPU 参数对照及 GPU 损失测试，不宣称 CUDA 参数逐字节一致。
CUDA 稀疏池化反向存在原子累加，运行间允许浮点差异。吞吐实验的损失差异仅在末几位打印精度。
原始数据、冻结输入及汇总在 `data/perf/prefetch/`，汇总文件 `results.json`。

复测示例（prepare 仅用于不存在的独立输出路径）：

```powershell
cargo build --profile fast --example perf_probe
.\target\fast\examples\perf_probe.exe prepare data/perf/prefetch/model.safetensors data/perf/prefetch/replay.jsonl
$env:GO9_TRAIN_PACK_WORKERS = "4"
.\target\fast\examples\perf_probe.exe train-loaded data/perf/prefetch/model.safetensors data/perf/prefetch/replay.jsonl 256 1 81920
Remove-Item Env:GO9_TRAIN_PACK_WORKERS
```


## 棋盘热点重写 · 2026-09-15

本轮重写候选落子分析：每个局面从现有棋块列表建立一次索引，使用 u128 位集合合并相邻己方棋块和气、识别被提棋块，并直接返回提子数与落子后气数。
特征提取及规则合法着列表共用此实现。候选超级劫检查使用栈上棋盘并精确比较历史，避免每个候选的堆分配、重复棋块遍历及额外气统计。
这是局面内共享分析，尚未实现跨搜索节点增量维护棋块；实际执行单步落子保留原算法，同时作为差分测试参照。

基于本地现有 128 宽度模型和 50000 条回放的固定快照，关闭 profile，优化前后可执行文件交替串行运行三次，以下均为中位数：

| 基准 | 本轮前 | 本轮后 | 吞吐提升 |
| --- | ---: | ---: | ---: |
| 单线程 100 次搜索 / 40000 次访问 | 930.880 ms | 649.926 ms | 43.2%（1.43 倍） |
| 128 线程、200 次固定局面搜索 | 358171 访问/s | 402073 访问/s | 12.3% |
| 独立训练、batch=256 | 19522 样本/s | 27544 样本/s | 41.1% |

单线程三次耗时：优化前 970.407 / 921.572 / 930.880 ms，优化后 649.926 / 618.674 / 669.277 ms。
128 线程三次吞吐：优化前 351430 / 379271 / 358171，优化后 413717 / 402073 / 399873 访问/s。
训练三次吞吐：优化前 19633 / 18003 / 19522，优化后 27544 / 27627 / 27302 样本/s。
128 线程短基准只运行 200 次搜索，受调度及固定局面分布影响；它不是用户日志中的 128 actors 完整流水线，也不是同一训练阶段模型，不能据此换算日志中的实际吞吐。
本轮未达到额外 4 倍加速，单线程收益没有等比例转化为高并发收益。

验证：48 个单元测试和 1 个 GTP 进程测试通过；新增 12 条确定性随机轨迹、每条最多 180 手，逐点逐颜色对照原落子实现的合法性、被提棋子集合及落子后气数。
5 个基准局面的特征、推理概率和价值、400 模拟搜索结果在新旧可执行文件之间逐字节一致。
模型格式、棋规和搜索预算不变，模型及回放文件 SHA256 与文末记录一致；可继续使用现有模型。
原始结果及冻结输入位于 `data/perf/board-rewrite/`，汇总为 `results.json`。


## 已完成优化

- 累加器只计算当前行棋方，保留原来的浮点加法顺序和 v30 参数格式。
- 特征中的合法落点平面复用于搜索策略和训练掩码，避免重新逐点试下。
- 特征提取直接指定视角，避免深拷贝整局历史；棋块分析共享给 Benson 活棋判断。
- 隐藏宽度 128 时，每节点累加器 arena 从 400 个 float 降至 281 个，减少 29.75%；这不是整个搜索树的内存降幅。

使用保存的优化前可执行文件与优化后版本交替串行运行，均关闭 profile，配置与输入完全相同，三次中位数：

| 测量 | 优化前 | 优化后 | 改善 |
| --- | ---: | ---: | ---: |
| 100 次搜索、40000 次访问 | 1991.310 ms | 946.967 ms | 吞吐 2.10 倍，耗时减少 52.4% |
| 训练 batch=256 | 15472 样本/s | 19722 样本/s | 吞吐增加 27.5% |

搜索原始耗时分别为 1980.036 / 1999.020 / 1991.310 ms 和 946.967 / 978.260 / 926.012 ms。
训练吞吐分别为 15472 / 12003 / 15803 与 13959 / 19739 / 19722 样本/s；训练测量波动较大，第一轮优化后更慢，27.5% 仅是本组三次中位数差异。
这些是固定局面搜索和独立训练基准，不能直接当作完整自博弈流水线加速比。
原始数据在 `data/perf/paired-*.txt`，汇总在 `data/perf/optimization-results.json`。

验证：`cargo test --profile fast --no-fail-fast`，47 个单元测试及 1 个 GTP 进程测试全部通过，包含 CUDA 训练/推理一致性。
新增 8 条确定性对局轨迹逐步比对两个视角的缓存合法落点与棋规接口，并检查双停着终局。
5 个基准局面的完整特征、策略概率、价值、400 次模拟搜索结果，优化前后 JSON 输出逐字节相同。
训练基准各轮最终 loss 都为 3.99199（打印精度）。模型及回放 SHA256 与文末一致，现有模型可以继续使用，无需重新训练。

仍可继续优化的热点包括落子后的棋块/气分析、逐点试下以及 CPU 训练打包；本次没有改变搜索预算、线程数、batch 或死活判定规则。

## 优化前诊断（历史记录）

## 测量方法

- AMD Ryzen 9 9950X，16 核 / 32 线程；NVIDIA RTX 5070 Ti，16 GB。
- 使用现有 128 隐藏宽度模型及 50,000 条真实回放，模型路径 `go9-v30-model.safetensors`。
- 搜索每次 400 次模拟，cpuct=2、cpuct_log=1.5；根对称 4，启用图搜索，匹配当前主要搜索参数。
- 从空棋盘以及回放中最接近 20、50、80、110 手的盘面取 5 个固定局面，每个局面搜索 20 次。
- 训练从回放按间隔抽取 2048 条，复用训练会话，计时 5 轮，共 10240 条样本。
- 先预热，再清零计时器。带 profile 的计时用于拆分热点；未开启 profile 的独立进程用于吞吐对照。
- 对照场景逐个运行；搜索参数和 batch 对照各取 3 次中位数。线程数对照每档运行 200 次搜索一次，只反映趋势。
- 模型加载、回放读取、CUDA 会话初始化不计入吞吐。计时过程中训练只修改内存模型，不保存覆盖生产模型。
- 多层计时是包含子调用的时间，表中“其中”行不能相加。GPU 相关项是主机端调用/等待耗时，
  CUDA 异步执行会影响归属，不能当作 CUDA kernel 独占时间。

## 搜索热点

100 次搜索共 40000 次实际访问，带计时总耗时 2105.4 ms。

| 环节 | 总计 ms | 搜索占比 | 调用数 |
| --- | ---: | ---: | ---: |
| 累加器完整重建 | 1565.8 | 74.4% | 33,340 |
| 其中：围棋特征及输入投影 | 1458.6 | 69.3% | 33,340 |
| 其中：特征编码 | 1086.3 | 51.6% | 66,680 |
| 其中：逐点落子分析 | 757.9 | 36.0% | 66,680 |
| 策略评估重新枚举合法着 | 247.2 | 11.7% | 31,980 |
| 终局死活分析 | 42.0 | 2.0% | 8,520 |
| MCTS 棋盘复制 | 23.4 | 1.1% | 32,840 |

策略 logit、价值头和激活合计约 6.8%，不是当前最大的环节。
本组未计时搜索耗时中位数为 2040.4 ms，说明插桩对整个基准的影响约为几个百分点。

### 1. 每个节点计算两份累加器，只使用一份

[model.rs](src/model.rs:464) 每次节点创建均完整重建 black/white 两套输入；
[model.rs](src/model.rs:495) 为两个视角各调用一次 `features::encode`，再遍历特征做输入投影。
但 [model.rs](src/model.rs:536) 只读取当前行棋方的切片；子节点再次独立重建，另一方缓存没有在子节点复用。
33340 次累加器构建对应 66680 次特征编码，实测印证了这一点。

**优先优化：只构建本节点行棋方的累加器。** 若保留双视角结构，则至少共享两方相同的棋块、气和 Benson 分析。
具体加速倍数尚未通过实现验证，不能直接声称会翻倍。

### 2. 合法性、提子和气数反复遍历棋盘

[features.rs](src/features.rs:40) 遍历 81 个交点，逐一执行 `placed`。
[game.rs](src/game.rs:144) 会复制棋盘、搜索相邻棋块和自身棋块，并检查超级劫历史；
返回后 [features.rs](src/features.rs:48) 为自紧气特征又搜索一次新棋块。
随后 [game.rs](src/game.rs:188) 为策略候选重新做一轮合法落子枚举。

**优先优化：每个局面共享棋块编号、气、提子数、落子后气数和合法掩码。**
先消除重复工作，再考虑固定大小数组、位集合和增量维护；不要一开始就改棋规或删掉超级劫检查。

### 3. 复制与终局分析不是本组的首要热点

MCTS 直接复制棋盘约 1.1%，特征准备中的 `for_turn` 克隆约 2.3%，终局死活分析约 2.0%。
它们可以后续优化，但优先级低于特征与投影。本组未覆盖所有复杂死活搜索的最坏情况。

## 训练热点

batch=256，预热后 10240 条样本，插桩实测 764.4 ms。

| 环节 | 总计 ms | 本次训练占比 |
| --- | ---: | ---: |
| CPU 样本打包 | 405.3 | 53.0% |
| 前向至标量回读 | 67.7 | 8.9% |
| 反向调用 | 152.7 | 20.0% |
| 优化器调用 | 99.2 | 13.0% |
| 张量构建与 H2D 调用 | 18.3 | 2.4% |
| 发布模型到 CPU | 9.9 | 1.3% |

[pack](src/candle_train.rs:701) 串行生成每条样本的特征；
第 724 行调用 `features::encode` 后，第 768 行再次生成搜索候选，重复合法性判断。
**训练优先复用这些中间结果，再测 CPU 打包并行/缓存。** 20 个自博弈线程已经占用 CPU，
打包并行不能直接再开满 32 线程；缓存浮点特征也会增加回放内存占用。

旧 `train.tensor_h2d` 的计时作用域一直延伸到反向完成，容易被误读为上传耗时；
本轮已把传输调用与前向/反向拆开。实测传输调用约 2.4%，模型发布约 1.3%，都不是主要瓶颈。

## 不带插桩的吞吐对照

### 搜索参数

同样 100 次搜索，时间越低越快。根对称变动也会改变策略与搜索路径，不是等棋力比较。

| 设置 | 中位总耗时 ms | 相对基线耗时减少 |
| --- | ---: | ---: |
| search | 2040.4 | +0.0% |
| graph-off | 2030.5 | +0.5% |
| symmetry-one | 2009.8 | +1.5% |

关闭图搜索和减少根对称在这组只有约 0.5%、1.5% 的差距，接近运行波动，不应作为首要优化。

### 搜索线程数

| 线程数 | 访问数/秒 |
| ---: | ---: |
| 1 | 19,128 |
| 4 | 65,980 |
| 8 | 141,332 |
| 16 | 220,045 |
| 20 | 212,166 |
| 32 | 253,829 |

这是固定局面并行搜索，不含自博弈生成、GPU 训练、队列及 Arena。
16 与 20 线程相近，32 线程本次更快，但不能据此直接把生产自博弈线程拉满，需给打包等工作留 CPU。

### 训练 batch

| batch | 样本/秒中位数 | 三次范围 |
| ---: | ---: | ---: |
| 64 | 6,303 | 5,167–8,262 |
| 256 | 12,192 | 12,159–13,384 |
| 1024 | 20,282 | 19,302–20,417 |

1024 在吞吐上更好，但相同样本量下优化器步数减少，收敛行为会改变；本轮未修改 batch 或学习率。
按当前每步 400 次模拟估算，自博弈对 CPU 的需求仍明显高于训练端的样本消费能力，
优先提升搜索端特征效率更有价值。这是基于微基准的推断，不是完整训练流水线利用率测量。

## 复现

```powershell
cargo run --profile fast --example perf_probe --features profile -- search
cargo run --profile fast --example perf_probe --features profile -- train
cargo run --profile fast --example perf_probe -- search
cargo run --profile fast --example perf_probe -- graph-off
cargo run --profile fast --example perf_probe -- symmetry-one
cargo run --profile fast --example perf_probe -- parallel go9-v30-model.safetensors data/go9-v30/replay.jsonl 20
cargo run --profile fast --example perf_probe -- train go9-v30-model.safetensors data/go9-v30/replay.jsonl 1024
```

不要同时运行多个基准进程。原始输出保存在本地 `data/perf/`（已被 Git 的 data 规则忽略）。

输入文件 SHA256（测试前后一致）：

- 模型：`C1B42E98228EE6039CF1FFCA1EC13C1A5FFDFF3F5213F68DF351D013B5FCF567`
- 回放：`66D44F433145F8610BFCDD2DDFE97BDC75BAAC3ABC1EAAAEFC07F656CF6A449A`
