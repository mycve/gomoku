# 19 路性能优化 · 2026-09-15

## 已实现及最终验证

本次保持模型 v32、回放字段、围棋规则、800 次搜索预算和训练参数不变。没有降低精度、删历史、取消超级劫，也没有省略死活特征。现有 v32 模型可以继续使用。

- 棋盘历史改为 `im::Vector` 与 `Arc` 共享的持久化结构，新增节点只分配新增历史及容器修改路径。
- 超级劫使用增量 Zobrist 哈希查持久化索引；索引值仅存历史位置编号。哈希命中后精确核对盘面，碰撞另存编号集合，维持原语义。
- 六字固定集合直接进行字级合并与计数；仅发生提子时合并己方棋块集合。
- 预计算四邻点，棋块和气在同一次 BFS 中构建，避免重复遍历与逐棋块清零。
- 模型加载和每次训练权重同步后预合并落子/行/列/对角线权重；输入投影改成连续切片遍历，便于 SIMD。保持原浮点运算次序。尚未实现完整父子节点增量特征维护。
- 搜索节点缓存终局结果；终局和已有转置节点不再额外构建累加器。

### 速度与内存

本机 AMD Ryzen 9 9950X（16 核 / 32 线程），Windows，`fast`、`target-cpu=native`，计时比较均未开启 profile。同一独立随机初始化 hidden=128 模型、同一批固定局面、每次 800 模拟。

| 局面手数 | 优化前模拟/秒 | 优化后模拟/秒 | 加速比 |
|---|---:|---:|---:|
| 0 | 16122 | 22752 | 1.41× |
| 100 | 6780 | 20895 | 3.08× |
| 250 | 5379 | 22100 | 4.11× |
| 400 | 6150 | 31084 | 5.05× |
| 500 | 5916 | 33878 | 5.73× |

上述每个局面连续运行 20 次搜索。五个局面总耗时从 11.63 秒降到 3.18 秒，约 3.66×。

16 线程每轮搜索 200 次，新旧程序交替运行三轮：

| 轮次 | 优化前模拟/秒 | 优化后模拟/秒 |
|---|---:|---:|
| 1 | 29674 | 141675 |
| 2 | 32303 | 143201 |
| 3 | 30992 | 141605 |
| 中位数 | 30992 | 141675 |

中位吞吐约 **4.57×**。同场景另测进程峰值工作集（50 ms 轮询系统 PeakWorkingSet）：旧版约 1653.8 MiB，新版约 488.4 MiB，减少约 70%。这包含模型、回放载入与搜索树，不能解读为单一历史结构大小。

以上为本地固定局面压力测试，不是 Ubuntu 上 128 actor 的完整自博弈吞吐保证。后者仍需相同模型、相同配置实测。

### 正确性

- 60 项单元测试和 1 项 GTP 进程测试通过，包含长棋局索引/线性历史对照、历史分叉独立性、碰撞精确核对、19 路提子/劫/对称性，以及非零权重缓存与刷新测试。
- 两套训练模型（hidden=16，以及本次 CUDA 训练后的 hidden=128）分别在五个局面上比较新旧程序。特征、网络输出、800 模拟下每个候选的访问次数、Q 和 prior 序列化结果逐字节一致。
- 独立 hidden=128 小配置完成两轮自博弈、CUDA 训练、每轮四局 Arena，并成功恢复到第 3 轮；正式模型和回放未改动。

### 运行

Ubuntu 同步源码后重新编译，继续使用原配置：

```sh
cargo build --profile fast
./target/fast/go19 az-loop
```

诊断命令见下方历史记录；多线程复现使用 `perf_probe parallel <model> <replay> 16`。正式吞吐对比不要启用 `--features profile`。

---

## 回放抽样优化与逻辑检查

`sample=` 精确计时 `sample_mixed_recent`，包含加权抽样和对称变换，不包含后续训练打包。旧实现每抽一条样本就逐张变换其全部历史并重建超级劫索引，最后打乱完整 Sample。这是几十秒延迟的主要结构性原因。

本次改动：

- 历史保持原坐标并共享，只记录当前视角的对称编号。旋转/镜像只变换当前、上一张盘面与 policy，历史不再重建。
- 超级劫查找将候选哈希映射回原坐标；命中后精确核对盘面。继续落子、组合旋转/镜像、序列化和反序列化均保持原语义。
- 先生成 `(样本索引, 对称编号)`，打乱小索引后再构建样本；近期/历史分区合并为一次遍历。
- 不增加抽样线程，避免与自博弈抢 CPU；随机数消耗顺序、抽样配额、最终顺序保持不变。模型及回放格式不变。

逻辑检查：近期窗口为 5 轮时，第 3 轮所有样本都属于近期，`actual_recent=100%` 是预期的历史不足回退。历史充足时会按配置保留近期配额。加权抽样预构建 CDF 并二分查找，复杂度为 O(池大小 + 抽样数 × log 池大小)，没有逐次全池扫描。

本地同一 533 条、0～532 手测试棋谱回放、相同 seed 抽 4096 条：旧版 0.845310 秒，新版 0.005445 秒，约 155×。完整样本序列化摘要都为 `4a16321293cc8e90`，不是仅核对数量。

再将上述回放循环克隆扩展到 246145 条，抽 81920 条，分段计时如下（开启 profile，毫秒）：

| 阶段 | 时间 |
|---|---:|
| 近期/历史分区 | 1.892 |
| 权重 CDF 构建 | 1.229 |
| 加权选择及对称编号 | 4.068 |
| 打乱索引 | 0.431 |
| 构建样本 | 155.396 |
| 抽样函数总计 | 163.515 |

扩展池共享测试棋谱的历史；这不是服务器真实 246145 条回放，也未叠加 128 actor 压力，不能把 0.164 秒当作服务器承诺。分段计时不含测试池构造及可选的结果序列化摘要。

验证：62 项测试通过，覆盖全部 64 种对称组合、与完整历史变换的对照、变换后继续落子和提子/超级劫、序列化恢复、CUDA 训练与推理一致性；五个固定局面的新旧 MCTS 指纹一致。独立小配置成功恢复并完成第 4 轮自博弈、CUDA 训练和 Arena。

复现分段计时（Linux）：

```sh
GO19_PROBE_POOL_SIZE=246145 cargo run --profile fast --features profile --example perf_probe -- sampling data/go19-probe-model.safetensors data/go19-probe-replay.jsonl 81920
```

小批结果核对可改成 `4096 digest`。不设置 `GO19_PROBE_POOL_SIZE` 时直接使用提供的回放池。

## 优化前诊断（历史记录）

## 范围

在本地 Windows、fast 编译配置下测试。独立随机初始化 hidden=128 模型，4 次模拟自博弈生成一局 533 条样本；从中取 100/250/400/500 手局面，加空棋盘。每个局面预热后重复搜索 20 次，每次 800 模拟。探针 cpuct=2、log=1.5、base=500，其他沿用 SearchConfig 默认值。不覆盖正式模型和回放，不代表 Ubuntu 服务器或已训练模型的性能。

## 搜索计时

下表为带 profile 插桩的累计耗时除以同局面搜索总时间，存在嵌套，不能相加。

| 局面手数 | 模拟/秒 | 完整累加器重建 | 其中候选分析 | 棋盘深拷贝 | NN 求值 | 终局分析 |
|---|---:|---:|---:|---:|---:|---:|
| 0 | 15505 | 61.4% | 28.3% | 0.4% | 31.9% | 0% |
| 100 | 6724 | 73.4% | 48.0% | 7.0% | 13.2% | 0% |
| 250 | 4899 | 69.5% | 36.7% | 14.1% | 6.5% | 0.2% |
| 400 | 5849 | 48.6% | 13.6% | 23.9% | 4.0% | 9.0% |
| 500 | 5641 | 42.3% | 9.0% | 30.1% | 3.7% | 7.0% |

主瓶颈随局面变化：中盘是特征构建，长历史后期深拷贝占比明显上升；普通 MCTS 子节点选择不是本次主瓶颈。空盘到后期并非单调变慢，剩余空点数和终局访问次数也影响耗时。

## 隔离历史长度实验

不带 profile 插桩，每项重复 1000 次。保留同一局面的其他字段，仅把诊断副本的历史缩为当前盘面一项。该副本会改变超级劫语义，只用于成本隔离，绝不能用于训练或实际搜索。

| 手数 | 原始特征编码 μs | 短历史特征编码 μs | 原始 clone μs | 短历史 clone μs |
|---|---:|---:|---:|---:|
| 100 | 77.89 | 33.66 | 2.59 | 0.080 |
| 250 | 83.12 | 39.20 | 6.66 | 0.082 |
| 400 | 33.98 | 16.71 | 9.42 | 0.080 |
| 500 | 24.26 | 13.79 | 12.35 | 0.081 |

历史扫描使此组局面的特征编码耗时达到短历史的约 1.8～2.3 倍。微测试 clone 立即释放，不能替代搜索树中持续保留大量克隆的计时。

## 对应代码与原因

1. `game::Board` 用 `Vec<Vec<i8>>` 保存历史，派生 Clone；`mcts::simulate` 每个新节点深拷贝历史，树中各节点各存一份。500 个历史盘面仅棋子数据就约 180.5 KB/节点。若 128 个 actor 各保留 800 个这种节点，理论上仅这部分约 18.5 GB，尚未计入分配器、Vec、边和模型；这是容量估算，不是实测 RSS。
2. `MoveAnalysis::placement` 对每个合法候选构造 361 点盘面，再逐项比较整局历史，以检查位置超级劫。每节点特征编码扫描所有交点，历史越长，重复比较越多。
3. `accumulator_into_arena` 实际调用完整 `accumulator`，未从父节点增量更新；每次遍历盘面、重建棋块/Benson/候选特征，再把非零输入投影到 hidden=128。`evaluate_incremental` 的名称并不意味着这些步骤已经增量化。
4. 搜索访问双 pass 节点时，`outcome` 会调用完整终局分析。后期有约 7～9% 时间花在这里；同一终局节点的结果可以缓存。

建议顺序：共享不可变历史和精确超级劫索引（哈希命中后仍核对盘面）→ 增量维护棋块及输入累加器 → 缓存终局结果。不能通过删历史、关闭超级劫或跳过死活特征来宣称等价优化。128 actor 的实际内存带宽、分配器竞争和最佳并发数还需要服务器实测。

## 复现

使用独立且尚不存在的路径生成诊断输入：

```sh
cargo run --profile fast --features profile --example perf_probe -- prepare data/go19-probe-model.safetensors data/go19-probe-replay.jsonl
cargo run --profile fast --features profile --example perf_probe -- search data/go19-probe-model.safetensors data/go19-probe-replay.jsonl
cargo run --profile fast --example perf_probe -- micro data/go19-probe-model.safetensors data/go19-probe-replay.jsonl
```

也可传入同格式的实际模型及已保存回放。`GO19_PROBE_SIMS` 控制探针模拟数，默认 800。新增计时点仅在 profile feature 开启时生效。本次没有修改搜索算法或正式训练参数。
